import 'package:flutter/material.dart';

class SearchAndDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search and Detail Page')),
      body: Center(child: Text('This is the Search and Detail Page')),
    );
  }
}
