import 'package:flutter/material.dart';

class ScanRFIDPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Scan RFID Page'),
      ),
      body: Center(
        child: Text('This is the Scan RFID Page'),
      ),
    );
  }
}